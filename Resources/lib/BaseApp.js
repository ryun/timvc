

var AppClass = Klass.extend({

	init: function() {
		this. = ;
	}

});



var UI = Klass.extend({

	init: function() {
		// get config options
	},

});

var Logger = Klass.extend({

	init: function() {
		this. = ;
	},

});

var Controller = AppClass.extend({
	init: function(opts) {
		this.controllers = opts.controllers || {};
		this._super( false );
	},
	
	
});


var View = Person.extend({
  init: function(){
    this._super( false );
  },
});


